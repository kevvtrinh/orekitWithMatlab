# Third-party dependencies

Keep only dependencies shared by multiple modules here. Integration-specific
manifests, licenses, checksums, and binaries belong inside that integration; Orekit
JARs therefore belong under `integrations/orekit/lib/` and are ignored by default.
