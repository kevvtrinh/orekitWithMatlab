# Tests

Root tests cover product composition and independent scientific validation. Unit,
regression, and provider-integration tests live inside the module or integration
they test, so each module can be validated independently.
