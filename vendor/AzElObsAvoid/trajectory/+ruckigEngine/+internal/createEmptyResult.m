function result = createEmptyResult(initialState, terminalState, limits, options, pathConstraints)
%% Section 0: Header & Readme
% SYNTAX
%   result = ruckigEngine.internal.createEmptyResult( ...
%       initialState, terminalState, limits, options, pathConstraints)
%
% PURPOSE
%   - Create the stable direct Ruckig-engine result format.
%
% INPUTS
%   - initialState, terminalState, limits, options, pathConstraints
%       Fully normalized Ruckig request records.
%
% OUTPUTS
%   - result (scalar struct)
%       Stable empty motion, validation, and diagnostics fields.
%
% UNITS
%   - Empty histories retain the request's coordinate and time conventions.
%

%% Section 1: Create Stable Failure Record

dimensionCount = numel(initialState.position);
result         = struct("Success", false, ...
    "Message", "The Ruckig-derived engine has not completed.", ...
    "TerminationReason", "notRun", ...
    "Inputs", struct("initialState", initialState, ...
        "terminalState", terminalState, ...
        "limits", limits, ...
        "pathConstraints", pathConstraints), ...
    "Options", options, ...
    "time", zeros(0, 1), ...
    "position", zeros(0, dimensionCount), ...
    "velocity", zeros(0, dimensionCount), ...
    "acceleration", zeros(0, dimensionCount), ...
    "jerk", zeros(0, dimensionCount), ...
    "ControlJerk", zeros(0, dimensionCount), ...
    "Polynomial", struct(), ...
    "FinalTime", NaN, ...
    "Duration", NaN, ...
    "IntegratedSquaredJerk", Inf, ...
    "MaximumConstraintViolation", Inf, ...
    "Validation", struct("Passed", false, ...
        "Message", "No trajectory is available."), ...
    "Diagnostics", struct());
end
