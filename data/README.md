# Runtime data

Place Orekit data, optional kernels, and large catalogs in this directory. Runtime
data is intentionally not committed; setup tooling should download or validate
versioned datasets and record their checksums.

