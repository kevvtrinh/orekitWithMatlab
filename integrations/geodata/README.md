# Geospatial data integration

Optional providers for country boundaries, coastlines, administrative areas, and
other named Earth features from versioned GeoJSON or datasets such as Natural Earth.
The `modules/geospatial` API does not depend on a particular dataset.

